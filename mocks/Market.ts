export class Market {
	runtimeData;
	intents;
	register;
	constructor(runtimeData, intents, register) {
		this.runtimeData = runtimeData;
		this.intents = intents;
		this.register = register;
		throw new Error("Method not implemented.");
	}
}
